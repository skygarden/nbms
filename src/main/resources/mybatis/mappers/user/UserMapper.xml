<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nexblue.nbms.web.mapper.UserMapper">
    <select id="findAll" resultType="User">
        SELECT
            COMP_CD,
            USER_ID,
            USER_NM,
            PASS_ID,
            HQ_CD,
            ARM_CD,
            DEPT_CD,
            POSITION_CD,
            RANK_CD,
            EMP_ID,
            EMAIL,
            HP_NO,
            WORK_STS,
            USE_YN,
            AUTH_LV,
            CREATE_DATE,
            CREATE_USER,
            UPDATE_DATE,
            UPDATE_USER
        FROM NBMS_USER_INF
    </select>

</mapper>